rules_version = '2';

// Basic Firebase Storage rules - allow all for development
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true; // Allow all access for development
    }
  }
}